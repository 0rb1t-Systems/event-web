# Event24 Web App

This file is leftover Lovable “Event Spark” documentation. It is **not** the product source of truth.

Use `.agent` for architecture, routes, env, API rules, mock vs real data, and recent changes.

- Frontend: Vite + React SPA
- Backend: Event24 Laravel API (`VITE_API_BASE_URL` + `X-API-Key`)
- Do not restore Supabase, HMAC signatures, or a second backend
- Preserve the surviving Lovable UI
